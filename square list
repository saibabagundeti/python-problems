def square_list(p_list):
    square_list = []
    for item in case_list:
        square_list.append(item ** 2)
    return square_list


case_list = [1, 2, 3, 4, 5]
result = square_list(case_list)
print(result)  # TODO
