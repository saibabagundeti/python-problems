def findDuplicate(arr):
    n = len(arr)
    for i in range(1,n-1):
        for j in range (i+1,n):
            if arr[i] == arr[j]:
                return arr[i]


arr = findDuplicate([1, 3, 4, 2, 2])
print(arr)
